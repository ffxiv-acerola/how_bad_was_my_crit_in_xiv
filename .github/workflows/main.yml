name: CI/CD Pipeline

on:
  push:
    branches: [ main, cicd ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -

    - name: Install dependencies
      run: poetry install

    # - name: Run tests
    #   run: poetry run pytest

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/cicd'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Generate config.py
      run: |
        cat << EOF > crit_app/config.py
        from pathlib import Path

        FFLOGS_TOKEN = "${{ secrets.FFLOGS_TOKEN }}"
        DB_URI = Path("data/reports.db")
        BLOB_URI = Path("data/blobs")
        DEBUG = False
        DRY_RUN = False
        BASE_PATH = Path("")
        ERROR_LOGIN_DATA = ${{ secrets.ERROR_LOGIN_DATA }}
        DASH_AUTH_SECRET = "${{ secrets.DASH_AUTH_SECRET }}"
        EOF

    - name: Generate potencies.csv
      run: |
        poetry install
        poetry run python fflogs_rotation/job_data/potencies/create_potencies.py

    - name: Set up SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

    - name: Deploy to production
      run: |
        rsync -avz --delete \
          -e "ssh -i ~/.ssh/id_rsa" \
          --exclude '.git*' \
          --exclude 'tests' \
          --exclude '.pytest_cache' \
          --exclude '__pycache__' \
          --exclude '.ruff_cache' \
          ./ ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.DEPLOY_PATH }}/

    # - name: Restart services
    #   run: |
    #     ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'sudo supervisorctl restart crit_app crit_app_errors'
